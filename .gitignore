node_modules/

# Build artifacts generated by `npm run build`.
# The build/ directory IS committed so the plugin works without a build step.
# This entry only suppresses the npm-lifecycle debug log.
npm-debug.log*
